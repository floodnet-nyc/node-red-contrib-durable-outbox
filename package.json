{
  "name": "node-red-contrib-durable-outbox",
  "version": "0.8.0",
  "description": "Durable SQLite outbox nodes for Node-RED",
  "main": "nodes/outbox.js",
  "type": "commonjs",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test test/*.test.js",
    "test:stress": "node --test test/stress/*.stress.js",
    "test:postgres": "node --test test/integration/*.integration.js"
  },
  "keywords": [
    "node-red",
    "outbox",
    "sqlite",
    "retry",
    "dead-letter"
  ],
  "license": "MIT",
  "node-red": {
    "nodes": {
      "durable-outbox": "nodes/outbox.js"
    }
  }
}
